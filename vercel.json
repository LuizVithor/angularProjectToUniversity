{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "yarn build && yarn start-json-server" }
    ],
    "routes": [
      { "src": "/api/db.json", "dest": "/api/db.json" }
    ]
  }